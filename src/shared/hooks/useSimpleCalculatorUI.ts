// Re-export from new domain location - legacy alias kept for backward compatibility
export { useCalculatorUI as useSimpleCalculatorUI } from '@/domains/calculator/hooks/useCalculatorUI';
export { useCalculatorUI as useRapidCalculatorUI } from '@/domains/calculator/hooks/useCalculatorUI';
