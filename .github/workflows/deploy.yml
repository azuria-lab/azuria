name: Deploy (Placeholder)

on:
  workflow_dispatch:
  push:
    branches: [ main ]

permissions:
  contents: read
  id-token: write  # Necessário para OIDC (futuro Azure)

jobs:
  deploy:
    name: Deploy Placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      - name: Install
        run: npm ci
      - name: Build
        run: npm run -s build
      - name: Futuro Login Azure
        run: echo "Adicionar passo de login Azure (az login / OIDC) aqui"
      - name: Futuro Deploy
        run: echo "Implementar pipeline de deployment (ex - upload para storage ou container app)"
      - name: Artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist
          if-no-files-found: error
      - name: Placeholder Notice
        run: echo "Workflow de deploy ainda não configurado para Azure."
