# ===========================================
# AZURIA - Configuração de Ambiente
# ===========================================
# Copie este arquivo para .env e preencha os valores
# ATENÇÃO: API keys sensíveis (Gemini, NIM) devem estar
# APENAS no Supabase Dashboard → Project Settings → Secrets

# ------------------------------------------
# SUPABASE (Obrigatório)
# ------------------------------------------
VITE_SUPABASE_URL=https://seu-projeto.supabase.co
VITE_SUPABASE_ANON_KEY=sua-chave-anonima-aqui

# ------------------------------------------
# NOTA SOBRE IA
# ------------------------------------------
# As chaves de API da IA (Gemini, NIM) NÃO devem estar aqui.
# Configure-as diretamente no Supabase Dashboard:
# Project Settings → Edge Functions → Secrets
#
# Chaves a configurar no Supabase:
# - GEMINI_API_KEY (obter em: https://aistudio.google.com)
# - SUPABASE_URL (copiada automaticamente)
# - SUPABASE_SERVICE_ROLE_KEY (copiada automaticamente)

# ------------------------------------------
# Configurações Opcionais
# ------------------------------------------
VITE_APP_ENV=development
VITE_AI_DEBUG_MODE=false
