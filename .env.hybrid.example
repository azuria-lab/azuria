# Hybrid (Cloud Auth + Local Data) preset
# Copy to .env.hybrid and fill your real values, or use as-is with the switcher to create .env.local

# Modo híbrido: autenticação na cloud, dados locais
VITE_SUPABASE_MODE=hybrid

# ============================================
# CLOUD - Autenticação (Supabase Cloud)
# ============================================
# URL do seu projeto Supabase Cloud
VITE_SUPABASE_CLOUD_URL=https://YOUR_PROJECT_REF.supabase.co
# Public anon key do Supabase Cloud
VITE_SUPABASE_CLOUD_ANON_KEY=YOUR_SUPABASE_CLOUD_ANON_KEY

# ============================================
# LOCAL - Dados (Supabase CLI/Docker)
# ============================================
# URL da instância local do Supabase
VITE_SUPABASE_LOCAL_URL=http://localhost:54321
# Anon key da instância local (obtida ao executar `supabase start`)
VITE_SUPABASE_LOCAL_ANON_KEY=YOUR_LOCAL_ANON_KEY

# ============================================
# OPCIONAL - URLs específicas
# ============================================
# Se você precisa separar explicitamente auth e data:
# VITE_SUPABASE_AUTH_URL=https://YOUR_PROJECT_REF.supabase.co
# VITE_SUPABASE_DATA_URL=http://localhost:54321

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. Certifique-se de que o Supabase local está rodando:
#    supabase start
#
# 2. Os usuários criados na cloud NÃO existem automaticamente no local
#    Você precisará sincronizar o schema e dados conforme necessário
#
# 3. Vantagens do modo híbrido:
#    - Auth gerenciado na cloud (recuperação de senha, etc)
#    - Desenvolvimento rápido com dados locais
#    - Sem custos de queries durante desenvolvimento
#
# 4. Para trocar entre modos, use:
#    npm run env:hybrid   # Ativa modo híbrido
#    npm run env:cloud    # Ativa modo cloud puro
#    npm run env:local    # Ativa modo local puro
